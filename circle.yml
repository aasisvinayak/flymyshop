machine:
  timezone:
    US/Pacific
  php:
    version: 5.6.2
  hosts:
    localhost: 127.0.0.1
  environment:
    ENVIRONMENT: testing-ci
    DB_DATABASE: testing.sqlite
  test:
    post:
      - php vendor/bin/phpunit